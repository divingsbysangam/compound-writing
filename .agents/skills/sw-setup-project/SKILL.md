---\nname: sw-setup-project\ndescription: Initialize a clean writing home with required context files.\n---\n\n# Setup Project

Initializes a writing workspace with `VOICE.md`, `STYLE.md`, `DOMAIN.md`, `examples/`, and `drafts/`.

## Process
1. Verify existing context files.
2. Ensure directory conventions (`context/drafts/<piece-slug>/`) are ready for notes, scaffolds, and revisions.\n